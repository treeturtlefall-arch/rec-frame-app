# REC枠 透過素材貼り付けソフト 仕様書

最終更新: 2026-09-18 / 対象コード: `rec_frame_app.py` / 状態: 開発中
最終目標: GitHub で OSS として公開（それまで微調整・機能追加を継続）

## 1. 概要・目的

- 任意サイズの画像に、カメラのファインダー風オーバーレイ（四隅L字枠＋電池マーク＋赤丸REC）を合成する簡易GUIアプリ。
- 透過PNGを引き伸ばして貼る方式ではなく、**画像サイズから相対値でオーバーレイをベクター生成**するため、どんな解像度でも比率が崩れない。

## 2. 機能（現段階）

| 機能 | 内容 |
|---|---|
| 画像を開く | PNG / JPG / JPEG / WebP / BMP。EXIF Orientation を正規化して開く（外部ビューアー表示と一致） |
| 枠色切替 | 黒 / 白 ラジオボタン（枠・電池・REC文字が連動、赤丸は常に赤）。変更でプレビュー即時更新 |
| 枠太さ切替 | 小 / 中 / 大 ラジオボタン（四隅L字枠のみ、電池・RECは不変）。変更でプレビュー即時更新 |
| 簡易/詳細モード | 簡易（全ON・十字・日時なし）/ 詳細（＋枠・電池・REC・中央十字・日時の5チェック＋日時手入力欄＋日時選択Spinbox、日時と十字は既定OFF）ラジオ切替。簡易時は「表示・日時」タブを隠し、詳細時に再表示して自動選択する。詳細値は往復しても保持。枠太さは両モード共通表示。変更でプレビュー即時更新 |
| RECフォント切替 | 10種＋カスタムのコンボボックス（§5）。変更でプレビュー即時更新 |
| エフェクト切替 | 3種ドロップダウン（§3.6）。両モード共通・変更でプレビュー即時更新 |
| プレビュー | 左設定・右プレビューの編集デスク構成。暗い表示域に全体をフィット（比率維持）。ウィンドウリサイズに150msデバウンスで追従。情報欄に `保存:WxH / 表示:WxH` を表示 |
| 保存 | PNG / JPEG（quality=95, RGB変換）/ WebP。初期ファイル名 `{元名}_rec{拡張子}` |
| 一括処理 | 「一括処理...」ボタンで入力/出力フォルダを選択し現在の設定で一括合成。CLI `python rec_frame_app.py --batch 入力 出力 [--config 設定.json]` も同等（省略時は自動記憶）。命名 `{元名}_rec{拡張子}`・衝突時は `_rec_01` 連番。対象外スキップ＋件数レポート。撮影日時モードON時はファイル毎に日時追従（§3.5） |
| 内蔵サンプル | Git管理外写真や同梱素材に依存せず、Pillowで中立的なベース画像を生成して既存の描画正本へ渡す。サンプル読込後も通常画像と同じ設定・保存経路を使用 |
| 日英切替 | ヘッダーの `日本語 / English` で即時切替。GUI、ネイティブダイアログ、バッチ進捗・結果、CLIを翻訳し、次回起動時に復元。既存・欠落設定は日本語。`--lang ja|en` は保存値より優先。画像へ描くREC・日時、ユーザー入力、ファイル名、カスタムフォント名は変更しない |

### 2.1 ウィンドウと操作領域

- 初期クライアントサイズは最大1160×700。タスクバーを除く作業領域に合わせて縮小し、最小目安は960×600（作業領域がさらに小さい場合はそこまで縮小）。
- 上部の開く・保存、左下の一括処理は固定し、設定タブ内だけを縦スクロールする。内容が収まる場合はスクロールバーを隠し、小画面で必要になれば再表示する。
- Tab移動で隠れた入力へ進むと自動で表示位置までスクロールする。ホイールは設定欄内だけに作用し、プレビューやコンボボックスの操作には干渉しない。
- 画像未読込時は保存ボタンを無効化。`Ctrl+O` / `Ctrl+S` に対応する。自動日時中は手入力・選択入力を無効化し、表示値は写真毎の解決日時と同期する。
- 言語切替はウィジェットを再生成せず、読込画像・入力値・選択タブ・スクロール位置を保持する。右上の識別ラベルは切替前でも英語話者が認識できるよう、両言語とも `Language` で固定する。組込フォント・エフェクト・太さは内部キーと翻訳表示名を分離し、言語によって保存設定や描画結果を変えない。

## 3. オーバーレイ仕様

- 基準スケール `s = min(幅, 高さ)`。全寸法は `s` または辺長の相対値。
- 出力は入力と同寸の RGBA。ベースを RGBA 化して `alpha_composite`。

### 3.1 四隅L字枠

| 項目 | 値 |
|---|---|
| 端からの距離 | 上下左右とも `int(s*0.028)` で等距離（640x360なら10px）。右・下端はPIL矩形のinclusive描画分を `-1` 補正し四隅ピクセル一致 |
| 腕の長さ | `int(s*0.16)` |
| 線の太さ | 小 `max(2, int(s*0.005))` / 中 `max(2, int(s*0.008))`（従来値・既定）/ 大 `max(2, int(s*0.012))`。電池線幅・REC丸は連動しない。短辺360pxなら小2/中2/大4px、1080pxなら小5/中8/大12px |
| 描画法 | 各隅を横棒・縦棒の2矩形で描画（角が綺麗） |

### 3.2 電池マーク（左上・枠内側）

| 項目 | 値 |
|---|---|
| 本体 | 幅 `s*0.075` × 高 `s*0.045`、線幅 `max(2, s*0.005)` |
| 位置 | `x=margin+s*0.02`、`y=margin+thickness+s*0.015` |
| 突起 | 幅 `本体幅*0.12`、高 `本体高*0.45`、垂直中央 |
| 残量バー | 4本、内幅いっぱいに均等配置（ピッチ=内幅/4、バー幅=ピッチ×0.5）、上下対称 |

### 3.3 赤丸＋REC（右上・枠内側）

| 項目 | 値 |
|---|---|
| フォントサイズ | `max(10, int(s*0.055))` |
| 赤丸半径 | `int(s*0.022)`、丸と文字の間隔 `int(s*0.012)` |
| 配置 | 全体幅を測って右寄せ。`y=margin+thickness+s*0.008+rec_dy`（`rec_dy=int(s*0.01)`＝短辺360pxで約3px下げ） |
| 中心合わせ | 行高 `max(インク高, 直径)` の中心線 `cy` に丸・文字インクとも配置。文字は `anchor="lt"` でタイト上端基準にし `cy - text_h//2` に置く（`lm` は行ボックス中央のためフォント依存の浮きが出る） |

### 3.4 中央十字（詳細限定・既定OFF）

| 項目 | 値 |
|---|---|
| 位置 | 中心 `(w//2, h//2)` |
| 片腕 | `int(s*0.035)` |
| 線幅 | `max(1, int(s*0.002))`（枠太さに連動しない独立値。枠より一回り細く） |
| 色 | 枠色に連動（赤丸は常に赤のまま） |

### 3.5 日時表示（右下・詳細限定・既定OFF・手入力）

| 項目 | 値 |
|---|---|
| 内容 | 手入力テキストを単一行でそのまま描画（例 `2026/09/14 PM 08:30`＝`YYYY/MM/DD AM/PM HH:MM`）。空・空白のみは描画スキップ。手入力欄への直接編集に加え、選択式（§10.3）からの合成も受け付ける |
| フォントサイズ | `max(10, int(s*0.04))`（RECの`0.055`より一回り小さく） |
| フォント | RECと共通 `font_key` |
| 色 | 枠色に連動（赤丸は常に赤のまま） |
| 位置 | 右端 `x=w-margin-int(s*0.02)`、下端 `y=h-1-margin-thickness-int(s*0.008)-rec_dy`（`rec_dy=int(s*0.01)`を上下対称の隙間に再利用）。`anchor="rb"` で右寄せ |
| ON/OFF | `show_timecode: bool=False`、`timecode_text: str=""`。簡易モードではOFF扱い（入力値は保持し詳細復帰で復元） |
| 撮影日時モード | `timecode_from_exif: bool=False`（既定OFF）。ON時は固定文の代わりに写真毎の日時を `EXIF(DateTimeOriginal→Digitized→DateTime)→ファイルmtime→固定文` の順で解決する。EXIFを持たないPNGスクショ等もmtimeで追従できる。バッチではファイル毎に日時追従。GUIでONにすると日時表示も連動ONになり、開いた画像の解決文が入力欄へ反映される（値変化時のみset）。`is_simple_equivalent` は本旗ONを非等価（詳細推定）とする |

### 3.6 UI劣化エフェクト（両モード共通・既定OFF）

| 項目 | 値 |
|---|---|
| 目的 | 静的ベクターUIに液晶・CRT特有の質感を非破壊で付加。原本ファイルは変えず、UIオーバーレイ層にのみ加工してから `alpha_composite` |
| 適用順序 | RGBシフト→ブルーム→低解像度化で固定（順序変更不可。見た目が変わるため） |
| RGBシフト | Rを左へ・Bを右へ。基準量 `d=max(1, int(s*0.002))`、弱×1 / 強×2。G・A固定。`ImageChops.offset` は反対端に回り込むため不使用、`Affine`変換＋`fillcolor=0` で帯クリア |
| ブルーム | ぼかし複製を背後に敷き原UIを重ねる。弱は枠色で分岐（白 `radius=max(1, int(s*0.006))`・発光層α190 / 黒は少し抑えて `radius=max(1, int(s*0.005))`・α160）/ 強 `radius=max(1, int(s*0.008))`・α220 |
| 低解像度化 | 弱=1/2。縮小 `BILINEAR`（細線消失を抑える）→等倍復元 `NEAREST` |
| プリセット | `OFF(クリーン)`=なし / `OLED(微発光)`=ブルーム弱 / `2000sデジカメ`=シフト弱＋低解像弱。内部キー `off/oled/2000s`。CRT/VHSは画面全体への破壊処理となりコンセプトに合わないため不採用 |
| 全要素OFF時 | エフェクトは無操作で透明のまま（合成＝ベース一致） |
| 設定 | `effect: EffectKey="off"`＋`custom_fonts: dict={}`＋`timecode_from_exif: bool=False`＋`ui_language="ja"`。JSONは `schema_version=8`。欠落時は各既定値を使用。枠太さ・言語は両モード共通設定で保存対象（`mode`自体は対象外） |
| 性能 | フルサイズ `GaussianBlur` をプレビュー毎に実行するため大画像では重くなる。既存150msデバウンスを再利用し、新規スロットルは設けない |

## 4. 起動・環境

- 依存: Python 3 + Pillow（`.venv` は Pillow 12.1.0 同梱）+ tkinter（標準）。
- 起動: `.venv\Scripts\python rec_frame_app.py`（または `python rec_frame_app.py`）。
- 配布exe（Windows）: GitHub Releases の `rec-frame-app.exe` をDLしてダブルクリック（Python不要）。`v*` タグpushで自動ビルド（`.github/workflows/release-exe.yml`＋`rec-frame-app.spec`）。GUIとCLI `--batch` 兼用のため `console=True` 維持（`windowed=True` 化は禁止）。
- 対応OS: Windows 想定（フォントパスは `C:/Windows/Fonts` 優先、Linuxは DejaVu にフォールバック）。

## 5. RECフォント候補（組込10種＋カスタム）

組込: Arial(標準) / Arial Narrow Bold(細長) / Arial Black(極太) / Impact(凝縮テロップ風) / Bahnschrift(工業的) / Consolas(等幅) / Roboto Condensed / Montserrat Medium / Bebas Neue(縦長) / OCRB(計測機器風)

カスタム: 所持フォント（.ttf / .otf / .ttc）を「追加...」で登録できる。選択した実体は設定ルート直下の `fonts/` へ複写し、`custom_fonts: {表示名: 複写先絶対パス}` として保存する（元ファイルの移動・削除に強くするため）。表示名はファイルの stem を起点に、組込・登録済みとの衝突時は `名 2` / `名 3` …で一意化する。読込可否は追加時に `ImageFont.truetype(path, 20)` で確認し、不可なら登録しない。描画時は複写先を最優先で試し、欠落・破損時は組込フォールバック（DejaVu→`load_default`）で例外を出さない。組込名は削除不可、カスタムは確認後に登録解除＋複写ファイル削除する（使用中だった場合は既定フォントに戻す）。設定JSONは `schema_version=6` に拡張（欠落の `custom_fonts` は空読込）。`--config` のパスは機械依存のため、別PCへの持越し時は再追加が必要。

## 6. 検証済み

- 640x360, 512x512, 1024x1024, 1920x1080, 2048x3072, 300x500 で同寸出力・枠位置正常。
- 四隅の端距離がピクセル一致（640x360で四隅とも10/10）。
- 10フォント全描画OK、赤丸と文字インクの中心差±1px以内（640x360・1920x1080で回帰テスト）。
- プレビュー比率は保存画像と一致（1px丸めのみ）。
- 2026-09-16: Windowsローカルでpytest全210件通過。GUIとバッチの描画一致を固定日時・EXIF・mtime×3エフェクトの9パターンで自動確認（Tk画面が使えない環境ではこの9件をスキップ）。
- 同日のリファクタリング前後で360パターン（2サイズ×2色×3エフェクト×3太さ×10フォント）の描画ピクセル一致を作業時に確認。GUI手動操作・exe再ビルドは今回未実施。
- 2026-09-18: 採用B案を本番へ移植。Windows実画面で初期・詳細・960×600・最大化を撮影。1160×780の表示寸法は縦472×630／横814×611。ネイティブ保存／フォルダ選択ダイアログとOS表示倍率125/150%はユーザー実機確認で良好。
- 2026-09-18: 日英即時切替を追加。英語960×600・Tk倍率125/150%で折返し・スクロール・日時欄を確認。設定復元、日英ピクセル一致、日英CLI・バッチを含むpytest全220件成功。

## 7. 既知の制限

- Windows 前提（フォントパスのみ。他OSはフォールバック動作）。
- 日本語／英語UI。既定は日本語で、選択を自動記憶する。
- テストコードは pytest で整備（等距離・同寸出力・全フォント描画・設定記憶・バッチ・カスタムフォント・撮影日時・GUI/バッチ描画一致・本番UI状態遷移・日英切替・配布設定の静的検査。計220件。§6・§9 参照）。exe自体のビルド・出力一致確認は別途行う。
- 設定は自動記憶（`%APPDATA%/rec-frame-app/config.json`、UI追加なし）。枠色・フォント・枠太さ・エフェクト・要素ON/OFF・日時文を復元。`mode` 自体は対象外で `show_*` から簡易/詳細を推定。

## 8. 変更履歴

詳細は `Changelog.md` が正本（`[Unreleased]`＋`[0.1.0]`）。ここには残さない。

## 9. OSS公開に向けたロードマップ

- [x] 単体リポジトリ化（済：リポジトリ直下の `rec_frame_app.py` に移行）
- [x] `README.md`（英日、スクショ・使い方・対応形式。`assets/sample_rec.png` 付き）
- [x] `LICENSE`（MIT。フォント非同梱の旨はREADMEに明記）
- [x] `requirements.txt` / `pyproject.toml`（Pillowのみ）
- [x] テスト整備（pytest: 等距離・同寸出力・全フォント描画・設定記憶・バッチ・カスタムフォント・撮影日時・GUI/バッチ描画一致・本番UI状態遷移・日英切替・`test_dist.py`・`conftest.py` 共通ヘルパーの自動化。計220件。画面のない環境でのスキップは§6参照）
- [x] CI（GitHub Actions: test、Python 3.11/3.12 × Ubuntu/Windows。初回実行はpush後）
- [x] 配布形態の決定（PyInstaller単体exeに決定：正本.py＋Releases配布。pip/zipappは副手段。約17MB・署名なしのため初回SmartScreen注意）
- [x] Issue/PRテンプレート、 Contributing ガイド（`.github/ISSUE_TEMPLATE/`・`.github/pull_request_template.md`・`CONTRIBUTING.md`）
- [x] 日本語／英語UI切替（即時反映・自動記憶・CLI `--lang`）

## 10. 機能追加・微調整メモ（随時追記）

### 10.1 バッチモード（一括編集）

実装済み。仕様は §2「一括処理」行が正本。

- [ ] オーバーレイ単体（透過PNG）の書き出し ※凍結（誰でも扱える感覚とのトレードオフ）
- [ ] マージンのスライダー調整 ※凍結（誰でも扱える感覚とのトレードオフ）

### 10.2 簡易モード・詳細モード切替（実装済み）

実装済み。仕様は §2「簡易/詳細モード」行・§3.4・§3.5 が正本。

### 10.3 日時入力の選択式化（実装済み・手入力欄も併存）

実装済み。仕様は §3.5 が正本。入力値の検証は緩和し暦チェックなし（1800年・2月30日も許容）。
-
